# FIX: Upgraded pytest from 7.4.3 to 8.0.0 to resolve dependency conflict in CI.
# The CI environment attempted to install both pytest 7.4.3 (from requirements.txt) and pytest 8.0.0 (likely from a separate CI step or plugin).
# By aligning with 8.0.0 we avoid the conflict. Also added pytest-asyncio for async tests.
fastapi==0.110.0
uvicorn==0.27.0
sqlalchemy==2.0.25
aiosqlite==0.19.0
pydantic==2.5.0
pytest==8.0.0
pytest-asyncio==0.23.4
httpx==0.26.0
