version: "3.8"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite+aiosqlite:///./data/tasks.db
      - LOG_LEVEL=INFO
      - APP_ENV=production
    restart: unless-stopped